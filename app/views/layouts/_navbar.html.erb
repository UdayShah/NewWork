<nav id="mainNav" class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span> <%= image_tag ('menu-light.svg') %>
            </button>
            <%= link_to '', root_path, class: "navbar-brand page-scroll navbar-brand-custom" %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
              <% if logged_in? %>
                <li>
                  <%= link_to "Log out", logout_path, method: :delete %>
                </li>
              <% else %>
                <li>
                  <%= link_to 'Log In', login_path, class: "page-scroll" %>
                </li>
                <li>
                  <%= link_to 'Sign Up', signup_path, class: "page-scroll" %>
                </li>
              <% end %>
			  <% if logged_in? %>
			   <li>
                  <%= link_to 'Postings', postings_path, class: "page-scroll" %>
               </li>
			   	<li>
                  <%= link_to 'Profile', current_user, class: "page-scroll" %>
               </li>
				<% end %>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
